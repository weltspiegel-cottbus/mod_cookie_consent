{
  "name": "mod_cookie_consent",
  "version": "0.1.5",
  "description": "Cookie Consent Banner für Drittanbieter-Dienste (YouTube, etc.)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/weltspiegel-cottbus/mod_cookie_consent.git"
  },
  "type": "module",
  "scripts": {
    "build:js": "node .build/build.mjs --js",
    "build:css": "node .build/build.mjs --css",
    "build": "npm run build:js && npm run build:css",
    "package": "node .build/package.mjs",
    "release": "npm run build && npm run package",
    "changelog": "changelogen --output CHANGELOG.md --noAuthors",
    "release:patch": "changelogen --release --patch --push --noAuthors",
    "release:minor": "changelogen --release --minor --push --noAuthors",
    "release:major": "changelogen --release --major --push --noAuthors"
  },
  "devDependencies": {
    "changelogen": "^0.6.2",
    "esbuild": "^0.28.0",
    "lightningcss": "^1.28.3"
  }
}
